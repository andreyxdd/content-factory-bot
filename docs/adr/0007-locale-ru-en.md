# Primary language Russian and English only (v1)

Each Creator has one **primary language** (`en` or `ru`) set during first onboarding and changeable via `/settings`. It controls (1) bot command replies and system prompts shown in chat, and (2) the language of generated **draft options** and **research briefs**. Additional locales are out of scope until translation files and profile schema exist for them.
